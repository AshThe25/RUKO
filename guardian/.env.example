# Where the FastAPI relay is reachable from the browser.
# Use https:// and wss:// anywhere other than local development.
NEXT_PUBLIC_RUKO_RELAY_HTTP=http://localhost:8000
NEXT_PUBLIC_RUKO_RELAY_WS=ws://localhost:8000
